# Todo List

- [x] Create `odd_even.sh` file with bash shebang
- [x] Add input validation to check if argument is provided
- [x] Add regex validation for integer input (`^-?[0-9]+$`)
- [x] Implement even/odd check using `(( ))` arithmetic and `%` operator
- [x] Print "IS EVEN" or "IS ODD" based on result
- [x] Add error handling with exit code 1 for invalid inputs
- [x] Make script executable with `chmod +x`
- [x] Test with input `0` (expect "IS EVEN")
- [x] Test with input `7` (expect "IS ODD")
- [ ] Test with input `-4` (expect "IS EVEN")
- [ ] Test with input `abc` (expect error, exit 1)
- [ ] Test with no argument (expect error, exit 1)
